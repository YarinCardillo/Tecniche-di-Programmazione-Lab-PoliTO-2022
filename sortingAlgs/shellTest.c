#include <stdio.h>
#define N 5

int main(){

	int array[N] = {4, 7, 3, 2, 5};
	int temp, indexMin;

	for (int i = 0; i < N; ++i)
	{
		fprintf(stdout, "%d ", array[i]);
	}
	fprintf(stdout, "\n");

	for (int i = 0; i < count; ++i)
	{
		/* code */
	}










	for (int i = 0; i < N; ++i)
	{
		fprintf(stdout, "%d ", array[i]);
	}

	return 0;
}